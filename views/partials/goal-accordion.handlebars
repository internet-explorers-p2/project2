<div class="container-fluid" id="accordion-area">
  <div class="card mt-3">
    <div class="card-header" id="headingOne">
      <h2 class="mb-0">
        <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapse{{dataValues.id}}"
          aria-expanded="true" aria-controls="collapse{{dataValues.id}}">
          {{dataValues.title}}
        </button>
      </h2>
      {{!-- PROGRESS BAR --}}
      <div class="progress" style="height: 2px;">
        <div class="progress-bar" role="progressbar" style="width: 25%;" aria-valuenow="25" aria-valuemin="0"
          aria-valuemax="100"></div>
      </div>
    </div>

    <div id="collapse{{dataValues.id}}" class="collapse show" aria-labelledby="headingOne" data-parent="#accordionExample">
      <div class="card-body">
        {{> milestone-button}}

        {{!-- <div id="progressbar"></div>

        <a id="plus">+</a>
        <a id="minus">-</a>
        <p id="progressLabel">0%</p> --}}
      </div>
    </div>
  </div>
</div>